apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kong-portal-controller.fullname" . }}-test-connection"
  labels:
    {{- include "kong-portal-controller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kong-portal-controller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
