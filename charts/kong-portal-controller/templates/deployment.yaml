apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "kong-portal-controller.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kong-portal-controller.labels" . | nindent 4 }}
spec:
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "kong-portal-controller.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "kong-portal-controller.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "kong-portal-controller.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ include "kong-portal-controller.fullname" . }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: {{ .Values.image.url }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          env:
            - name: ingress-suffix
              value: {{ .Values.ingressSuffix }}
            - name: kong-token
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.kongTokenSecret }}
                  key: token
                  optional: false
            - name: kong-admin-url
              value: {{ .Values.kongAdminUrl }}
            - name: monitor-namespaces
              value: {{ .Values.monitorNamespacs }}

      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
